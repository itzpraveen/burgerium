---
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<section id="about" class="section-padding about-section">
    <div class="container">
        <div class="grid-layout">
            <!-- Left: Visual Stack -->
            <div class="visual-col reveal">
                <div class="img-frame">
                    <img src={`${base}/hero_feast.png`} alt="Burger Tree spread" class="main-img">
                    <div class="stat-box">
                        <span class="stat-num">25+</span>
                        <span class="stat-label">Years of <br>Legacy</span>
                    </div>
                </div>
            </div>

            <!-- Right: Story -->
            <div class="content-col reveal delay-200">
                <span class="badge">The Evolution</span>
                <h2 class="section-title">From Oven Heat to <span class="text-orange">Burgerium</span></h2>
                
                <p class="story-text">
                    <b>Burgerium</b> is our flagship — fueled by decades of dough, fire, and flavor. We started as <b>Konari Breads and Cakes</b> (1998), graduated to flame-grilled burgers with <b>Burger Tree</b> (2017), and now channel that heritage into Burgerium under <b>Urban Eats and Kitchen LLP</b>.
                </p>

                <div class="timeline-box">
                    <div class="timeline-step">
                        <div class="step-icon old-logo">
                            <img src={`${base}/burgertree_logo.png`} alt="Konari Breads and Cakes">
                        </div>
                        <div class="step-info">
                            <h4>Bakery Roots</h4>
                            <span>1998 • Calicut</span>
                        </div>
                    </div>
                    
                    <div class="connector-line"></div>

                    <div class="timeline-step">
                        <div class="step-icon old-logo">
                            <img src={`${base}/burgertree_logo.png`} alt="Burger Tree">
                        </div>
                        <div class="step-info">
                            <h4>Burger Tree</h4>
                            <span>2017 • Kerala & Tamil Nadu</span>
                        </div>
                    </div>
                    
                    <div class="connector-line"></div>
                    
                    <div class="timeline-step">
                        <div class="step-icon new-logo">
                        <img src={`${base}/logo.png`} alt="Burgerium">
                        </div>
                        <div class="step-info">
                            <h4>Burgerium</h4>
                            <span>2025 • Chennai</span>
                        </div>
                    </div>
                </div>

                <p class="story-footer">
                    <b>Burgerium</b> leads the story — modern, bold, and Chennai-bound — powered by the legacy that began in 1998. Always a unit of <b>Urban Eats and Kitchen LLP</b>.
                </p>
            </div>
        </div>
    </div>
</section>

<style>
    .grid-layout {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 5rem;
        align-items: center;
    }

    .img-frame {
        position: relative;
        border-radius: 8px;
        overflow: hidden;
    }

    .main-img {
        width: 100%;
        filter: grayscale(20%) contrast(110%);
        transition: 0.5s;
    }

    .img-frame:hover .main-img {
        filter: grayscale(0%);
        transform: scale(1.05);
    }

    .stat-box {
        position: absolute;
        bottom: 0;
        left: 0;
        background: var(--brand-orange);
        padding: 2rem;
        color: #000;
    }

    .stat-num {
        display: block;
        font-family: var(--font-heading);
        font-size: 3.5rem;
        line-height: 1;
    }

    .stat-label {
        font-weight: 700;
        text-transform: uppercase;
        font-size: 0.85rem;
    }

    .story-text {
        font-size: 1.15rem;
        color: #ccc;
        margin-bottom: 2.5rem;
    }

    /* Timeline Stylization */
    .timeline-box {
        background: #1a1a1a;
        padding: 2rem;
        border-radius: 12px;
        display: flex;
        align-items: center;
        width: fit-content;
        border: 1px solid rgba(255,255,255,0.05);
    }

    .timeline-step {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        gap: 0.5rem;
    }

    .step-icon img {
        height: 40px;
        width: auto;
    }
    
    .new-logo img {
        /* Ensure logo contrast */
        background: var(--brand-orange);
        padding: 4px 8px;
        border-radius: 4px;
    }

    .step-info h4 {
        font-size: 1rem;
        color: var(--text-white);
        margin: 0;
    }

    .step-info span {
        font-size: 0.75rem;
        color: var(--text-gray);
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .connector-line {
        width: 60px;
        height: 2px;
        background: var(--brand-orange);
        margin: 0 1.5rem;
        position: relative;
    }
    
    .connector-line::after {
        content: '';
        position: absolute;
        right: 0;
        top: -4px;
        width: 0; 
        height: 0; 
        border-top: 5px solid transparent;
        border-bottom: 5px solid transparent;
        border-left: 5px solid var(--brand-orange);
    }

    .story-footer {
        margin-top: 2rem;
        font-style: italic;
        color: var(--brand-orange);
    }

    @media (max-width: 900px) {
        .grid-layout { grid-template-columns: 1fr; gap: 3rem; }
        
        .img-frame { height: 300px; margin-bottom: 2rem; }
        .main-img { height: 100%; object-fit: cover; }
        
        .timeline-box { 
            width: 100%; 
            justify-content: space-between; 
            padding: 1.5rem;
        }
        
        .connector-line { width: 30px; margin: 0 0.5rem; }
        
        .section-title { font-size: 2.2rem; }
    }
</style>
