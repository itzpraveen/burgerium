---
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<section id="about" class="section-padding about-section">
    <div class="container">
        <div class="grid-layout">
            <!-- Left: Visual Stack -->
            <div class="visual-col reveal">
                <div class="img-frame">
                    <img src={`${base}/bg.png`} alt="Cafe Interior" class="main-img">
                    <div class="stat-box">
                        <span class="stat-num">25+</span>
                        <span class="stat-label">Years of <br>Legacy</span>
                    </div>
                </div>
            </div>

            <!-- Right: Story -->
            <div class="content-col reveal delay-200">
                <span class="badge">The Evolution</span>
                <h2 class="section-title">From <span class="text-orange">Roots</span> to <span class="text-orange">Revolution</span></h2>
                
                <p class="story-text">
                    It started in <b>1998</b> with a simple dream in <b>Calicut</b>: to serve burgers that feel like home. 
                    <b>The Burger Tree</b> became a cult favorite in <b>Coimbatore (Peelamedu & RS Puram)</b> and <b>Palakkad</b> for its 
                    freshly baked house-made buns and juicy, flame-grilled patties.
                    <i>Real food, made with love, served with pride.</i>
                </p>

                <div class="timeline-box">
                    <div class="timeline-step">
                        <div class="step-icon old-logo">
                            <!-- Placeholder for Old Logo Img or stylized text -->
                        <img src={`${base}/burgertree_logo.png`} alt="1998">
                        </div>
                        <div class="step-info">
                            <h4>The Roots</h4>
                            <span>1998 • Calicut</span>
                        </div>
                    </div>
                    
                    <div class="connector-line"></div>
                    
                    <div class="timeline-step">
                        <div class="step-icon new-logo">
                        <img src={`${base}/logo.png`} alt="2025">
                        </div>
                        <div class="step-info">
                            <h4>The Future</h4>
                            <span>2025 • Chennai</span>
                        </div>
                    </div>
                </div>

                <p class="story-footer">
                    Now launching <b>Burgerium</b> in Anna Nagar. The same soul, a bolder spirit.
                </p>
            </div>
        </div>
    </div>
</section>

<style>
    .grid-layout {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 5rem;
        align-items: center;
    }

    .img-frame {
        position: relative;
        border-radius: 8px;
        overflow: hidden;
    }

    .main-img {
        width: 100%;
        filter: grayscale(20%) contrast(110%);
        transition: 0.5s;
    }

    .img-frame:hover .main-img {
        filter: grayscale(0%);
        transform: scale(1.05);
    }

    .stat-box {
        position: absolute;
        bottom: 0;
        left: 0;
        background: var(--brand-orange);
        padding: 2rem;
        color: #000;
    }

    .stat-num {
        display: block;
        font-family: var(--font-heading);
        font-size: 3.5rem;
        line-height: 1;
    }

    .stat-label {
        font-weight: 700;
        text-transform: uppercase;
        font-size: 0.85rem;
    }

    .story-text {
        font-size: 1.15rem;
        color: #ccc;
        margin-bottom: 2.5rem;
    }

    /* Timeline Stylization */
    .timeline-box {
        background: #1a1a1a;
        padding: 2rem;
        border-radius: 12px;
        display: flex;
        align-items: center;
        width: fit-content;
        border: 1px solid rgba(255,255,255,0.05);
    }

    .timeline-step {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        gap: 0.5rem;
    }

    .step-icon img {
        height: 40px;
        width: auto;
    }
    
    .new-logo img {
        /* Ensure logo contrast */
        background: var(--brand-orange);
        padding: 4px 8px;
        border-radius: 4px;
    }

    .step-info h4 {
        font-size: 1rem;
        color: var(--text-white);
        margin: 0;
    }

    .step-info span {
        font-size: 0.75rem;
        color: var(--text-gray);
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .connector-line {
        width: 60px;
        height: 2px;
        background: var(--brand-orange);
        margin: 0 1.5rem;
        position: relative;
    }
    
    .connector-line::after {
        content: '';
        position: absolute;
        right: 0;
        top: -4px;
        width: 0; 
        height: 0; 
        border-top: 5px solid transparent;
        border-bottom: 5px solid transparent;
        border-left: 5px solid var(--brand-orange);
    }

    .story-footer {
        margin-top: 2rem;
        font-style: italic;
        color: var(--brand-orange);
    }

    @media (max-width: 900px) {
        .grid-layout { grid-template-columns: 1fr; }
        .timeline-box { width: 100%; justify-content: space-between; }
    }
</style>
