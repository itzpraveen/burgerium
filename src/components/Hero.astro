<section id="home" class="hero-section">
    <!-- Visual Layer -->
    <div class="hero-visual">
        <img src="/chennai_hero.png" alt="Chennai Crowd at Burgerium" class="hero-img">
        <div class="gradient-overlay"></div>
    </div>

    <!-- Content Layer -->
    <div class="container hero-container">
        <div class="hero-content reveal">
            <div class="hero-badges">
                <span class="badge">Est. 1998</span>
                <span class="badge" style="background:var(--text-white); color:#000; border:none;">Chennai Launch</span>
            </div>
            
            <h1 class="display-text">
                The Legend <br>
                <span class="text-stroke">Has Arrived.</span>
            </h1>
            
            <p class="hero-sub">
                From the roots of <b>Burger Tree</b> (Calicut • Coimbatore • Palakkad) comes an elevated dining experience. 
                Welcome to <span class="text-orange">Burgerium</span>.
            </p>
            
            <div class="cta-group">
                <a href="#menu" class="btn btn-primary">
                    Explore Menu <i class="fas fa-arrow-right"></i>
                </a>
                <a href="#about" class="btn btn-outline">
                    Our Roots
                </a>
            </div>
        </div>

        <!-- Floating Feature Card -->
        <div class="hero-feature reveal delay-200">
            <div class="product-card">
                <img src="/peri_burger.png" alt="Signature Peri Peri" class="burger-float">
                <div class="card-meta">
                    <span class="meta-label">Must Try</span>
                    <h4>Lava Cheese Peri Peri</h4>
                    <span class="meta-city">Coimbatore Favorite</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .hero-section {
        position: relative;
        height: 100vh;
        min-height: 700px;
        display: flex;
        align-items: center;
        overflow: hidden;
        padding-top: var(--header-height);
    }

    .hero-visual {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
    }

    .hero-img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        /* Slight zoom effect animation could go here */
    }

    .gradient-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: radial-gradient(circle at 70% 50%, rgba(0,0,0,0.4), var(--bg-dark) 90%);
    }

    .hero-container {
        display: grid;
        grid-template-columns: 1.2fr 0.8fr;
        align-items: center;
        width: 100%;
    }

    .hero-content {
        max-width: 700px;
    }

    .hero-badges {
        display: flex;
        gap: 1rem;
        margin-bottom: 1.5rem;
    }

    .text-stroke {
        -webkit-text-stroke: 2px var(--brand-orange);
        color: transparent;
        font-weight: 700;
    }

    .hero-sub {
        font-size: 1.25rem;
        opacity: 0.9;
        margin: 2rem 0;
        max-width: 500px;
        border-left: 3px solid var(--brand-orange);
        padding-left: 1.5rem;
    }

    .cta-group {
        display: flex;
        gap: 1.5rem;
    }

    /* Product Feature Card */
    .product-card {
        position: relative;
        background: rgba(20,20,20,0.8);
        backdrop-filter: blur(15px);
        border: 1px solid rgba(255,255,255,0.1);
        padding: 2rem;
        border-radius: 20px;
        text-align: center;
        transform: rotate(-3deg);
        transition: 0.4s ease;
    }

    .product-card:hover {
        transform: rotate(0) scale(1.02);
        border-color: var(--brand-orange);
    }

    .burger-float {
        width: 280px;
        margin: -4rem auto 1rem;
        filter: drop-shadow(0 20px 30px rgba(0,0,0,0.5));
    }

    .card-meta h4 {
        font-size: 1.5rem;
        color: var(--text-white);
        margin: 0.5rem 0;
    }

    .meta-label {
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 2px;
        color: var(--brand-orange);
        font-weight: 700;
    }

    .meta-city {
        font-size: 0.9rem;
        color: var(--text-gray);
        font-style: italic;
    }

    @media (max-width: 968px) {
        .hero-container { grid-template-columns: 1fr; text-align: center; }
        .hero-content { margin: 0 auto; }
        .hero-badges { justify-content: center; }
        .hero-sub { margin: 2rem auto; border: none; padding: 0; }
        .cta-group { justify-content: center; }
        .hero-feature { display: none; /* Hide floating burger on mobile for cleanliness */ }
        .gradient-overlay { background: rgba(0,0,0,0.7); }
    }
</style>
