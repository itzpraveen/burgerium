<section class="highlights section-padding">
    <div class="container highlights-grid">
        <div class="highlight-card scroll-reveal">
            <div class="icon-pill"><i class="fas fa-fire"></i></div>
            <div>
                <p class="eyebrow">Signature Bite</p>
                <h3>Lava Cheese Chicken</h3>
                <p class="muted">House-baked buns, molten cheese core, peri-peri kick.</p>
            </div>
        </div>
        <div class="highlight-card scroll-reveal delay-1">
            <div class="icon-pill"><i class="fas fa-location-dot"></i></div>
            <div>
                <p class="eyebrow">Locations</p>
                <h3>Palakkad • Coimbatore • Chennai</h3>
                <p class="muted">Tharekkad (Palakkad) plus RS Puram & Race Course (Coimbatore). Anna Nagar launches soon.</p>
            </div>
        </div>
        <div class="highlight-card scroll-reveal delay-2">
            <div class="icon-pill"><i class="fas fa-clock"></i></div>
            <div>
                <p class="eyebrow">Hours & Reservations</p>
                <h3>11am – 11pm</h3>
                <p class="muted">Reserve a table or order ahead. Call <a href="tel:+916362006008">6362 006 008</a>.</p>
            </div>
        </div>
    </div>
</section>

<style>
    .highlights {
        background: linear-gradient(135deg, rgba(250, 162, 39, 0.08), rgba(12, 12, 14, 0.95));
        padding-top: 4rem;
        padding-bottom: 4rem;
    }

    .highlights-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 1.5rem;
    }

    .highlight-card {
        display: grid;
        grid-template-columns: auto 1fr;
        gap: 1rem;
        padding: 1.5rem;
        border-radius: 14px;
        border: 1px solid var(--glass-border);
        background: rgba(20, 20, 23, 0.9);
        box-shadow: 0 10px 30px rgba(0,0,0,0.25);
    }

    .icon-pill {
        width: 44px;
        height: 44px;
        border-radius: 12px;
        display: grid;
        place-items: center;
        background: rgba(250, 162, 39, 0.18);
        color: var(--accent-orange);
        font-size: 1.2rem;
    }

    .eyebrow {
        letter-spacing: 1px;
        text-transform: uppercase;
        font-size: 0.75rem;
        color: var(--text-secondary);
        margin-bottom: 0.35rem;
    }

    .muted {
        color: var(--text-secondary);
        line-height: 1.5;
        font-size: 0.95rem;
    }

    .highlight-card h3 {
        margin-bottom: 0.4rem;
        font-size: 1.4rem;
    }

    .highlight-card a {
        color: var(--accent-orange);
        text-decoration: underline;
    }
    
    @media (max-width: 600px) {
        .highlights {
            padding-top: 3rem;
            padding-bottom: 3rem;
        }
    }
</style>
