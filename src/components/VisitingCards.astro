---
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<section class="section-padding cards-section" id="cards">
    <div class="container">
        <div class="cards-head">
            <div>
                <p class="eyebrow">Brand Stationery</p>
                <h2 class="section-title">Visiting Cards</h2>
                <p class="lede">
                    Two variants: a shop card for outlets and a personal/partner card. Both must keep
                    <strong>A Unit of Urban Eats and Kitchen LLP</strong> directly under the logo and leave room for a QR code.
                </p>
            </div>
            <div class="legal-note">LLP line is mandatory on every print run. Keep it legible — no hairline text.</div>
        </div>

        <div class="card-grid">
            <article class="vc-card shop-card scroll-reveal">
                <div class="vc-logo">
                    <img src={`${base}/logo.png`} alt="Burger Tree logo">
                    <span class="llp">A Unit of Urban Eats and Kitchen LLP</span>
                </div>
                <div class="vc-body">
                    <h3>Shop Card</h3>
                    <p>For counter display and outlet teams.</p>
                    <div class="info-block">
                        <span class="label">Outlet</span>
                        <strong>Tharekkad, Palakkad</strong>
                        <span class="muted">18/80, S.M. Complex, College Road, Palakkad - 678001</span>
                    </div>
                    <div class="info-block">
                        <span class="label">Contact</span>
                        <strong>+91 9544 600 900</strong>
                        <span class="muted">burgertree1@gmail.com</span>
                    </div>
                </div>
                <div class="qr-placeholder">QR</div>
            </article>

            <article class="vc-card partner-card scroll-reveal delay-100">
                <div class="vc-logo">
                    <img src={`${base}/logo.png`} alt="Burger Tree logo">
                    <span class="llp">A Unit of Urban Eats and Kitchen LLP</span>
                </div>
                <div class="vc-body">
                    <h3>Personal / Partner Card</h3>
                    <p>For leadership — include designation and email.</p>
                    <div class="info-block">
                        <span class="label">Name</span>
                        <strong>Your Name</strong>
                        <span class="muted">Managing Partner · you@burgertree.in · +91-00000-00000</span>
                    </div>
                    <div class="info-block">
                        <span class="label">Name</span>
                        <strong>Ash</strong>
                        <span class="muted">Managing Partner · ash@burgertree.in · +91-00000-00000</span>
                    </div>
                </div>
                <div class="qr-placeholder">QR</div>
            </article>
        </div>
    </div>
</section>

<style>
    .cards-section {
        background: linear-gradient(135deg, rgba(250, 162, 39, 0.08), rgba(12, 12, 14, 0.95));
    }

    .cards-head {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        gap: 2rem;
        margin-bottom: 3rem;
    }

    .legal-note {
        border: 1px solid rgba(250, 162, 39, 0.4);
        padding: 1rem 1.25rem;
        border-radius: 10px;
        background: #0b0b0b;
        max-width: 320px;
        font-weight: 700;
        color: #fff;
    }

    .card-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
    }

    .vc-card {
        background: #101010;
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 16px;
        padding: 1.5rem;
        position: relative;
        overflow: hidden;
        min-height: 320px;
    }

    .vc-card::after {
        content: '';
        position: absolute;
        inset: 0;
        background: radial-gradient(circle at 20% 20%, rgba(250, 162, 39, 0.2), transparent 45%);
        pointer-events: none;
    }

    .vc-logo {
        display: flex;
        flex-direction: column;
        gap: 0.2rem;
        margin-bottom: 1rem;
    }

    .vc-logo img {
        width: 140px;
    }

    .llp {
        font-size: 0.75rem;
        letter-spacing: 0.6px;
        text-transform: uppercase;
        font-weight: 800;
        color: var(--brand-orange);
    }

    .vc-body h3 {
        margin-bottom: 0.3rem;
        font-size: 1.4rem;
    }

    .vc-body p {
        color: #d3d3d3;
        margin-bottom: 1rem;
    }

    .info-block {
        margin-bottom: 0.9rem;
    }

    .label {
        display: block;
        text-transform: uppercase;
        letter-spacing: 1px;
        font-size: 0.75rem;
        color: #999;
        margin-bottom: 0.15rem;
    }

    .muted {
        color: #b9b9b9;
        font-size: 0.95rem;
        line-height: 1.5;
    }

    .qr-placeholder {
        position: absolute;
        bottom: 1rem;
        right: 1rem;
        width: 64px;
        height: 64px;
        border-radius: 8px;
        border: 1px dashed rgba(255, 255, 255, 0.4);
        display: grid;
        place-items: center;
        color: #fff;
        font-weight: 800;
        background: rgba(255, 255, 255, 0.03);
    }

    @media (max-width: 900px) {
        .cards-head {
            flex-direction: column;
        }

        .legal-note {
            max-width: 100%;
        }
    }
</style>
