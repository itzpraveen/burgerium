---
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<section id="menu" class="menu-section">
    <div class="container">
        <div class="header-center">
            <span class="sub-head">Taste The Hype</span>
            <h2 class="title-head">Crowd <span class="text-stroke">Favorites</span></h2>
        </div>

        <div class="spotlight-grid">
            <!-- Item 1: The Star -->
            <div class="spotlight-card featured">
                <div class="card-bg"></div>
                <div class="card-content">
                    <div class="card-header">
                        <h3>Lava Cheese</h3>
                        <span class="price">₹260</span>
                    </div>
                    <p>RS Puram's obsession. A molten cheese core exploding inside a crispy chicken patty. Warning: Highly Addictive.</p>
                    <button class="order-btn">Order Now</button>
                </div>
                <img src={`${base}/peri_burger.png`} alt="Lava Cheese" class="product-img float-slow">
            </div>

            <!-- Item 2: The Side -->
            <div class="spotlight-card">
                <div class="card-content">
                    <div class="card-header">
                        <h3>Cheesy Fries</h3>
                        <span class="price">₹140</span>
                    </div>
                    <p>The Palakkad Legend. Crispy golden shoestrings drowned in our secret rich cheese sauce.</p>
                </div>
                <img src={`${base}/feast.png`} alt="Fries" class="product-img scale-hover">
            </div>

            <!-- Item 3: The Classic -->
            <div class="spotlight-card">
                <div class="card-content">
                    <div class="card-header">
                        <h3>Smoky BBQ Beef</h3>
                        <span class="price">₹240</span>
                    </div>
                    <p>Flame-grilled juicy beef patty, caramelized lux-onions, and our thick house BBQ glaze.</p>
                </div>
                <!-- Reusing burger image as placeholder for variety, user can upload specific image later if needed -->
                <img src={`${base}/burger.png`} alt="Burger" class="product-img scale-hover">
            </div>
        </div>
    </div>
</section>

<style>
    .menu-section {
        background: #080808;
        padding: 8rem 0;
        overflow: hidden;
    }

    .header-center {
        text-align: center;
        margin-bottom: 5rem;
    }

    .sub-head {
        color: var(--brand-orange);
        text-transform: uppercase;
        letter-spacing: 3px;
        font-size: 0.9rem;
        display: block;
        margin-bottom: 1rem;
    }

    .title-head {
        font-size: 3.5rem;
        color: #fff;
    }

    .text-stroke {
        -webkit-text-stroke: 1px #fff;
        color: transparent;
    }

    /* Grid Layout */
    .spotlight-grid {
        display: grid;
        grid-template-columns: 1.5fr 1fr 1fr;
        grid-template-rows: 400px;
        gap: 2rem;
    }

    /* Card Styling */
    .spotlight-card {
        position: relative;
        background: #111;
        border-radius: 20px;
        padding: 2rem;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        overflow: hidden;
        border: 1px solid rgba(255,255,255,0.05);
        transition: 0.4s;
    }

    .spotlight-card:hover {
        border-color: var(--brand-orange);
        transform: translateY(-5px);
    }

    .spotlight-card.featured {
        background: linear-gradient(135deg, #1a1a1a 0%, #0d0d0d 100%);
    }

    .card-bg {
        position: absolute;
        top: 0; right: 0;
        width: 300px; height: 300px;
        background: radial-gradient(circle, rgba(250,162,39,0.2) 0%, transparent 70%);
        border-radius: 50%;
        transform: translate(30%, -30%);
    }

    .card-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 1rem;
        z-index: 2;
        position: relative;
    }

    .card-header h3 {
        font-size: 1.8rem;
        max-width: 150px;
        line-height: 1;
    }

    .price {
        font-family: var(--font-body);
        font-weight: 700;
        color: var(--brand-orange);
        font-size: 1.2rem;
    }

    .spotlight-card p {
        color: #888;
        font-size: 1rem;
        z-index: 2;
        position: relative;
        max-width: 60%;
    }

    .order-btn {
        background: #fff;
        color: #000;
        border: none;
        padding: 0.8rem 1.5rem;
        border-radius: 30px;
        font-weight: 700;
        cursor: pointer;
        margin-top: 1.5rem;
        width: fit-content;
        transition: 0.3s;
        z-index: 2;
        position: relative;
    }

    .order-btn:hover {
        background: var(--brand-orange);
        box-shadow: 0 0 15px rgba(250,162,39,0.5);
    }

    /* Image Handling */
    .product-img {
        position: absolute;
        bottom: -20px;
        right: -20px;
        width: 280px;
        transition: 0.5s ease;
        filter: drop-shadow(0 10px 20px rgba(0,0,0,0.5));
    }

    .spotlight-card.featured .product-img {
        width: 380px;
        right: -50px;
        bottom: -50px;
    }

    .spotlight-card:hover .product-img {
        transform: scale(1.1) rotate(-5deg);
    }

    .float-slow {
        animation: float 6s ease-in-out infinite;
    }

    @media (max-width: 1024px) {
        .spotlight-grid {
            grid-template-columns: 1fr;
            grid-template-rows: auto;
        }
        .spotlight-card { height: 350px; }
    }
</style>
---
const base = import.meta.env.BASE_URL;
---
