---
const base = import.meta.env.BASE_URL;
---

<section class="craft-section">
    <div class="container">
        <span class="sub-label">Our Secret Sauce</span>
        <h2 class="section-title">The <span class="text-orange">Art</span> of The Burger</h2>
        
        <div class="craft-grid">
            <!-- Card 1 -->
            <div class="craft-card scroll-reveal">
                <div class="card-visual">
                    <img src={`${base}craft_buns.png`} alt="Baking Buns">
                    <div class="overlay-gradient"></div>
                </div>
                <div class="card-info">
                    <h3>House-Made Buns</h3>
                    <p>We don't buy buns; we bake them. Fresh, every single morning. Soft, airy, and toasted to golden perfection.</p>
                </div>
            </div>

            <!-- Card 2 -->
            <div class="craft-card scroll-reveal delay-200">
                <div class="card-visual">
                    <img src={`${base}craft_grill.png`} alt="Flame Grilling">
                    <div class="overlay-gradient"></div>
                </div>
                <div class="card-info">
                    <h3>Flame Grilled</h3>
                    <p>No flat tops. Pure fire. Our patties are grilled over open flames for that signature smokey char you can't fake.</p>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .craft-section {
        background: #0c0c0c;
        padding: 6rem 0;
        position: relative;
    }

    .sub-label {
        display: block;
        text-align: center;
        color: var(--brand-orange);
        text-transform: uppercase;
        letter-spacing: 2px;
        font-size: 0.9rem;
        margin-bottom: 0.5rem;
    }

    .section-title {
        text-align: center;
        margin-bottom: 4rem;
        font-size: 3rem;
    }

    .craft-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 3rem;
    }

    .craft-card {
        background: #161616;
        border-radius: 16px;
        overflow: hidden;
        border: 1px solid rgba(255,255,255,0.05);
        transition: 0.3s;
    }

    .craft-card:hover {
        transform: translateY(-10px);
        border-color: var(--brand-orange);
    }

    .card-visual {
        height: 300px;
        position: relative;
        overflow: hidden;
    }

    .card-visual img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: 0.5s;
    }

    .craft-card:hover .card-visual img {
        transform: scale(1.1);
    }

    .overlay-gradient {
        position: absolute;
        bottom: 0; left: 0; width: 100%; height: 50%;
        background: linear-gradient(to top, #161616, transparent);
    }

    .card-info {
        padding: 2rem;
        position: relative;
    }

    .card-info h3 {
        font-size: 1.8rem;
        margin-bottom: 1rem;
        color: #fff;
    }

    .card-info p {
        color: #aaa;
        line-height: 1.6;
    }

    @media (max-width: 900px) {
        .craft-grid { grid-template-columns: 1fr; gap: 2rem; }
        .card-visual { height: 250px; }
    }
</style>
